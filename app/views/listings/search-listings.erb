<% @listings = Listing.where("name LIKE ? OR location LIKE ? OR item_category LIKE ?", "%#{params[:name]}%","%#{params[:location]}%","%#{params[:item_category]}%") %>
<% @listings.all.each do |listing| %>
<div class="col-md-12 item-listing">
  <img src="<% if listing.img_url == nil || listing.img_url == "" %>
    <%= "/images/default_listing_img.png" %>
    <% else %>
    <%= listing.img_url %>
    <% end %>" class="img-thumbnail" alt="Yom Kef" align="left" width="120">
<strong><h4><%= listing.name %></h4></strong><a href="/sessions/add-listing-item/<%= listing.id %>"> + Add Listing</a><br><br>
<strong>Location:</strong> <%= listing.location %><br>
<strong>URL:</strong> <a href="<%= listing.url %>"><%= listing.url %></a><br>
<strong>Category:</strong> <%= listing.item_category %><br>
<strong>Description:</strong> <%= listing.description %><br>
</div>
<% end %>
